{
    "version": 2,
    "builds": [
      { "src": "index.js", "use": "@vercel/node" }
    ],
    "scripts": {
      "build": "npx puppeteer browsers install chrome"
    },
    "routes": [
      { "src": "/(.*)", "dest": "/" }
    ]
  }